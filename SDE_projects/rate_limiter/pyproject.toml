[project]
name = "rate-limiter"
version = "0.1.0"
description = "A reusable token-bucket rate limiter for FastAPI"
dependencies = [
    "fastapi",
    "redis",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"